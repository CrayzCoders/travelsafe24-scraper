{
  "name": "travelsafe24-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Scrapes Kleinanzeigen rental listings for Hamburg, cleans them, geocodes, and publishes data/listings.json.",
  "scripts": {
    "scrape": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "browsers:install": "playwright install chromium-headless-shell"
  },
  "devDependencies": {
    "@types/node": "^20",
    "fingerprint-injector": "^2.1.82",
    "ghost-cursor-playwright": "^2.1.0",
    "playwright": "1.52.0",
    "rebrowser-playwright": "^1.52.0",
    "tsx": "^4.22.3",
    "typescript": "^5"
  }
}
